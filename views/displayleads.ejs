<% include header %>
   
<table class="table table-hover">
    <thead>
        <tr><th>Name</th>
            <th>score</th>
            <th>Creator</th>
        </tr>
    </thead>

    <tbody>
        <% entries.forEach(function(entry) { %>
        <tr>
            <td>
                <a href="displayleads/<%= entry.name %>">
                <%= entry.name %></a>
            </td>
            <td>
                <%= entry.score %>
            </td>
            <td>
                <%= entry.creator %>
            </td>

        </tr>
        <% }) %>
    </tbody>

    <% include footer %>